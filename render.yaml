services:
  - type: web
    name: telegram-info-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python insta.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
